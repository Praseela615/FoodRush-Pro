import React from "react";
import {useOrder} from "../context/OrderContext";
export default function CheckoutSummary(){const {order}=useOrder();if(!order)return null;return <section className="section-pad"><div className="confirmation-card"><div className="success-circle">✓</div><span className="eyebrow">ORDER CONFIRMED</span><h2>Thanks, {order.name}!</h2><p className="text-secondary">Order <b>{order.id}</b> is saved in this browser. Refreshing the page will not remove it.</p><div className="summary-box">{order.items.map(i=><div className="d-flex justify-content-between py-2" key={i.id}><span>{i.name} × {i.qty}</span><b>₹{i.price*i.qty}</b></div>)}<hr/><div className="d-flex justify-content-between"><strong>Total</strong><strong>₹{order.total}</strong></div></div><a className="btn btn-primary" href="#tracking">Track this order →</a></div></section>}